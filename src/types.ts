export type detailsProps = {
  phone: string;
  twitter: string;
  instagram: string;
  email: string;
  github: string;
};

export type projectProps = {
  id: string;
  name: string;
  video: string;
  link: string;
  description: string;
  image?: string;
  logo?: string;
  short?: string;
};

export type previewprops = { name: string; url: string };
